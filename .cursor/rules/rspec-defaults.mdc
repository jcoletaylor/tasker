---
description:
globs:
alwaysApply: false
---
When we run rspec tests, the command line switch `-v` does not mean verbose, it means
version, and so when we want verbosity, use `--format documentation`
